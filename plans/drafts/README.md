# Draft plans

Work-in-progress plans live here. Anything directly in `plans/` is asserted by
`tests/test_consistency.py` to have a matching application, so a plan cannot quietly drift out
of the application record. Drafts in this directory are exempt from that check.

Move a plan up into `plans/` when you apply with it.
